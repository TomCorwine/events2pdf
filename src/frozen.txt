    frozen = 'not'
    if getattr(sys, 'frozen', False):
        # we are running in a bundle
        frozen = 'ever so'
        bundle_dir = sys._MEIPASS
    else:
        # we are running in a normal Python environment
        bundle_dir = os.path.dirname(os.path.abspath(__file__))
    print( 'we are',frozen,'frozen', file=sys.stderr)
    print( 'bundle dir is', bundle_dir , file=sys.stderr)
    print( 'sys.argv[0] is', sys.argv[0] , file=sys.stderr)
    print( 'sys.executable is', sys.executable , file=sys.stderr)
    print( 'os.getcwd is', os.getcwd() , file=sys.stderr)
